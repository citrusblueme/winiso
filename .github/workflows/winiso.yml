name: WinISO

on:
  workflow_dispatch:
  
permissions:
  contents: write

jobs:
  winiso:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - run: unzip *.zip

      - run: ls

    
